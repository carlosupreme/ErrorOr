export enum ErrorType {
    failure = "failure",
    unexpected = "unexpected",
    validation = "validation",
    conflict = "conflict",
    notFound = "notFound",
    unauthorized = "unauthorized",
    forbidden = "forbidden",
}